# ===== Python / 构建产物 =====
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
dist/

# ===== 测试 / 临时 =====
.pytest_tmp/
.pytest_cache/
htmlcov/
.coverage
coverage.xml
.ruff_cache/

# ===== WorkBuddy 内部（含项目记忆，勿入库）=====
.workbuddy/

# ===== 生成产物（MIDI/WAV/MP3/metadata）=====
output/

# ===== QA 测试残留（fluidsynth 改名探针）=====
module/fluidsynth_qa_hidden/

# ===== 根目录 .mid 防御规则（触发条件已不复现）=====
# 历史遗留：早期 test_generators 的可复现性测试曾把 .mid 写到 CWD。
# 2026-09-20 复核：tests/test_generators.py 三例均已显式使用 tmp_path，
# 全量跑测后根目录无任何 .mid 残留（规则实际保护对象为空）。
# 保留作为防御网；若将来确实需要提交根目录 .mid，请先删掉本规则。
/*.mid

# ===== tests/ 调试产物（videomaker / render 手动冒烟输出，勿入库）=====
# 53 mp4 + 20 png + 11 wav + 1 mp3，均为调试时写出的中间产物；
# 已确认 29 个 tests/*.py 均未引用它们（test_videomaker 用的是 tmp_path 临时文件）。
# 以下规则只匹配 tests/ 下一层，不影响子目录——将来若要加测试 fixture，
# 请放 tests/fixtures/ 或 tests/data/ 子目录，不会被误忽略。
tests/*.mp4
tests/*.wav
tests/*.png
tests/*.mp3

# ===== 灵感库数据库（运行态生成，不入库）=====
smartnotegen.db

# ===== 系统文件 =====
desktop.ini
Thumbs.db

# ===== AI 权重 / DiffRhythm 仓库（可重新下载，不入库）=====
module/diffrhythm/
module/espeak-install.log
spike/
