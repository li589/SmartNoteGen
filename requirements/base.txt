# P0/P1-非AI 运行依赖（锁定精确版本，P2-3d）
# 不包含任何 torch / audiocraft / diffrhythm 等 AI 依赖（见 ai.txt）
typer==0.27.1
music21==10.5.0
pretty-midi==0.2.11.post0
midi2audio==0.1.1
soundfile==0.14.0
numpy==2.5.1
tomli-w==1.2.0

# 可选：MP3 导出（export --format mp3 / export suno --format mp3 时需安装）
lameenc==1.8.4
